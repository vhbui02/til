# TypeScript Configuration - `baseUrl`

1. Since TypeScript 4.1, `paths` doesn't require `baseUrl`, relative `paths` entries resolve against the directory containing the TypeScript configuration file.
2. TypeScript 6.0 deprecate `baseUrl` and displays error.
3. TypeScript 7.0 fully remove `baseUrl`.
